---
last_compound_scan: 2026-05-06
compound_scan_mode: full
---

# Service Topology

> Auto-generated by ttadk, last updated: 2026-05-06

## 目录

- [Overview](#overview)
- [Service Dependencies](#service-dependencies)
- [Interface Contracts](#interface-contracts)
- [Service Boundaries](#service-boundaries)
- [Source References](#source-references)

## Overview

本项目为前端单页应用，通过 HTTP API 与后端服务通信。开发环境使用 Vite 代理，生产环境直接请求配置的 API 基础地址。

## Service Dependencies

```
Frontend (React SPA)
    │
    ├── Auth Service (/api/auth/*)
    │   ├── POST /login
    │   └── POST /register
    │
    ├── Chat Service (/api/chat/*)
    │   ├── GET /sessions
    │   ├── POST /sessions
    │   ├── GET /sessions/:id
    │   ├── DELETE /sessions/:id
    │   └── POST /messages/stream
    │
    ├── Translate Service (/api/translate)
    │   └── POST /
    │
    ├── Profile Service (/api/profile/*)
    │   ├── GET /:language
    │   ├── POST /:language
    │   └── DELETE /:language
    │
    └── Digital Human Service (/api/digital-human/*)
        ├── GET /status
        └── POST /train
```

## Interface Contracts

### Auth Service

#### POST /api/auth/login

**Request:**
```typescript
// src/api/auth.ts:13-17
interface LoginParams {
  email: string
  password: string
}
```

**Response:**
```typescript
// src/api/auth.ts:19-23, 26-33
interface ApiResponse<LoginData> {
  message: string
  data: {
    id: number | string
    username: string
    email: string
    createdAt: string
    updatedAt?: string
    token?: string
  }
}
```

**Errors:**
| 状态码 | 说明 |
|--------|------|
| 400 | 请求参数错误 |
| 401 | 认证失败（邮箱或密码错误） |
| 500 | 服务器内部错误 |

#### POST /api/auth/register

**Request:**
```typescript
// src/api/auth.ts:36-41
interface RegisterParams {
  username: string
  email: string
  password: string
  confirmPassword: string
}
```

**Response:** 同登录响应

### Chat Service

#### GET /api/chat/sessions

**Request:** Query params `page`, `limit`

**Response:**
```typescript
// src/api/chat.ts:191-209
interface GetSessionsResponse {
  message: string
  data: ChatSession[]
  meta: {
    total: number
    page: number
    limit: number
  }
}
```

#### POST /api/chat/messages/stream

**Request:**
```typescript
// src/api/chat.ts:1-17
interface StreamChatParams {
  messages: ChatMessagePayload[]
  signal?: AbortSignal
  onChunk: (chunk: string) => void
  scenario?: string      // 格式: "一级场景/二级场景"
  language?: string      // cn-中文, jp-日文, kr-韩语, us-美式英语
  sessionId?: string
}
```

**Response:** 流式响应，支持 SSE 或 NDJSON 格式

**Stream Event Types:**
| 事件类型 | 说明 |
|----------|------|
| start | 流开始 |
| message/delta | 内容增量 |
| done/complete | 流结束 |
| error | 错误 |

**Errors:**
| 状态码 | 说明 |
|--------|------|
| 401 | 未授权，需重新登录 |
| 400 | 请求参数错误 |
| 500 | 服务器内部错误 |

### Translate Service

#### POST /api/translate

**Request:**
```typescript
// src/api/translate.ts:20-24
interface TranslateRequest {
  text: string
  targetLanguage?: 'zh' | 'en' | 'ja' | 'ko'  // 默认中文
}
```

**Response:**
```typescript
// src/api/translate.ts:26-36
interface TranslateResponse {
  translation: string
  pronunciation?: string
  example?: {
    sentence: string
    translation: string
  }
}
```

### Profile Service

#### GET /api/profile/:language

**Response:**
```typescript
// src/api/profile.ts:27-30
interface GetProfileResponse {
  message: string
  data: LanguageProfile | null
}
```

#### POST /api/profile/:language

**Request:**
```typescript
// src/api/profile.ts:22-26
interface CreateProfileRequest {
  level: LanguageLevel
  motivations: Motivation[]
  goals: Goal[]
  dailyTime: DailyTime
}
```

### Digital Human Service

#### GET /api/digital-human/status

**Response:**
```typescript
// src/api/digitalHuman.ts:5-9
interface DigitalHumanInfo {
  status: 'not_created' | 'training' | 'ready' | 'failed'
  frontendPicUrl?: string
  resourceId?: string
}
```

#### POST /api/digital-human/train

**Request:**
```typescript
// src/api/digitalHuman.ts:16-20
interface TrainDigitalHumanParams {
  vid: string
  alphaVid?: string
  interactionOptimise?: boolean
}
```

## Service Boundaries

**开发环境：**
- 前端运行在 Vite 开发服务器
- API 请求通过 `/api` 代理到 `http://localhost:3000`

**生产环境：**
- 前端静态文件部署
- API 请求发送到 `VITE_API_BASE_URL` 配置的地址

## Source References

| 服务 | 文件路径 | 主要函数 |
|------|----------|----------|
| Auth | src/api/auth.ts | login, register, isAuthenticated |
| Chat | src/api/chat.ts | streamChatMessage, getChatSessions, createChatSession |
| Translate | src/api/translate.ts | translateText |
| Profile | src/api/profile.ts | getProfile, upsertProfile, deleteProfile |
| Digital Human | src/api/digitalHuman.ts | getDigitalHumanStatus, createDigitalHumanTask |
| HTTP Client | src/utils/request.ts | get, post, put, del |
